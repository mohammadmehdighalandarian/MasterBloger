@page
@model IndexModel
@{
    ViewData["Title"] = "Index";
}

<main class="posts-listing col-lg-12">
    <div class="container">
        <div class="row">

            @foreach (var article in Model.Articles)
            {
                <div class="post col-xl-6">
                    <div class="post-thumbnail">
                        <a asp-area="Administrator" asp-page="./list" asp-route-id="@article.Id">
                            <img src="@article.Image" alt="@article.Titel" title="@article.Titel" class="img-fluid">
                        </a>
                    </div>
                    <div class="post-details">
                        <div class="post-meta d-flex justify-content-between">
                            <div class="date meta-last">@article.CreationDate</div>
                            <div class="category">
                                <a asp-page="/ArticleDetails" asp-route-id="@article.Id">@article.ArticleCategory</a>
                            </div>
                        </div>
                        <a asp-page="/ArticleDetails" asp-route-id="@article.Id">
                            <h3 class="h4">@article.Titel</h3>
                        </a>
                        <p class="text-muted">
                            @article.ShortDiscreption
                        </p>
                        @*<footer class="post-footer d-flex align-items-center">
                            <div class="comments meta-last"><i class="icon-comment"></i>@article.CommentsCount</div>
                        </footer>*@
                    </div>
                </div>
            }

        </div>
    </div>
</main>